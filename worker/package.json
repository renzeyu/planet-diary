{
  "name": "planet-diary-likes-worker",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "generate:ids": "node scripts/generate-planet-ids.mjs",
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "db:migrate:local": "wrangler d1 migrations apply DB --local",
    "db:migrate:remote": "wrangler d1 migrations apply DB --remote"
  },
  "devDependencies": {
    "wrangler": "4.112.0"
  }
}
